apiVersion: v1
kind: Service
metadata:
  name: ascoachingogvaner
  labels:
    app.kubernetes.io/name: ascoachingogvaner
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app.kubernetes.io/name: ascoachingogvaner
